@model IEnumerable<TestArtiest.Models.Artiest>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Toevoegen", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AantalLuisteraars)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Label)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AantalLuisteraars)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Label)
        </td>
        <td>
            @Html.ActionLink("Bewerken", "Edit", new { id=item.ArtiestId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtiestId }) |
            @Html.ActionLink("Verwijderen", "Delete", new { id=item.ArtiestId })
        </td>
    </tr>
}

</table>
